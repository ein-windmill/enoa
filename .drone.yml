#pipeline:
#  build:
#    image: maven:3.6-alpine
#    commands:
#      - mvn clean package -Dmaven.test.skip=true


---
kind: pipeline
name: master_pipe

steps:
  - name: install
    image: alpine
    commands:
      - ls /

trigger:
  branch:
    - dev

---
kind: pipeline
name: dev_pipe

steps:
  - name: package
    image: alpine
    commands:
      - ls /dev

trigger:
  branch:
    - dev
